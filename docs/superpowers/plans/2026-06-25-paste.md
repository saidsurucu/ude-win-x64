# Paste (#4) — Uygulama Planı

> **REQUIRED SUB-SKILL:** superpowers:executing-plans.

**Goal:** PASTERICH (stilli) + PLAINPASTE (formatsız) + PASTEIMG (imaj).

**Architecture:** Mac dosyalarını birebir kopyala; tek uyarlama: RichPaste.insertRtf→false (pbrich/textutil drop). Build-zamanı Javassist; bayraklar default-on. PASTEIMG empirik.

## Global Constraints
- Paketler `macospasterich`/`macospasteimage` KORUNUR. `javac --release 11 -encoding UTF-8`. Önce kopyala sonra uyarla.
- Referans: `$env:TEMP\ude-mac-ref\scripts\macos-pasterich`, `macos-pasteimage`.

---

### Task 0: Dal + jar + referans (feat/paste).

### Task 1: PASTERICH kopyala + RichPaste uyarla + apply
- [ ] **Step 1:** `scripts\pasterich\macospasterich\` altına 8 helper (HtmlToUde, NativeInsert, Css, Html, UdeDoc, UdeXml, PrLog, RichPaste, PlainPaste) + `scripts\pasterich\` altına PasteRichPatch.java, PlainPastePatch.java birebir kopyala.
- [ ] **Step 2: RichPaste uyarla** — `insertRtf` gövdesini (her iki overload) pbrich/textutil çağrılarını çıkarıp `return false;` yap + debug'da `PrLog.log` ile flavor listesi. `com.apple`/`ProcessBuilder`/`pbrich`/`textutil`/`/usr/bin` referansı KALMAMALI. (insertInto + diğer helper'lar DOKUNULMAZ.)
- [ ] **Step 3:** obfuscate doğrula: `hj.a(Transferable)`, `hj.paste()`, `text.fK` jar'da var mı (javap).
- [ ] **Step 4:** `apply-pasterich.ps1` (helper'ları derle+enjekte → PasteRichPatch derle+çalıştır+enjekte → PlainPastePatch). nativedialog deseni.
- [ ] **Step 5:** jar'a uygula; çıktıda enjekte mesajları; compile/run hata yok.
- [ ] **Step 6:** Commit.

### Task 2: PASTEIMG empirik karar + (gerekirse) port
- [ ] **Step 1:** YAMASIZ jar başlat, ekran-görüntüsü panoya al, belgeye Ctrl+V → imaj yapışıyor mu?
- [ ] **Step 2:** Yapışıyorsa PASTEIMG ATLA (spec'e not). Yapışmıyorsa: PasteImagePatch.java + Conv.java kopyala (`scripts\pasteimg\`), `wp.b.at` doğrula, `apply-pasteimg.ps1`, uygula.
- [ ] **Step 3:** Commit.

### Task 3: Bayraklar + build + paste testi
- [ ] **Step 1:** patch.ps1'e PASTERICH/PLAINPASTE (+PASTEIMG) blokları (default-on, =0 kapat); build.ps1 switch'leri.
- [ ] **Step 2:** `-Skin -Icons` + paste bayrakları ile tam build (UDE_VERSION ile).
- [ ] **Step 3: Word paste testi** — Word'de tablo+kalın+liste+renk kopyala → UDE'ye Ctrl+V → biçim korunur mu (ss). HtmlToUde bozarsa: PasteRichPatch'i `fragmentHtmlFlavor`'a çevir, yeniden test.
- [ ] **Step 4: PLAINPASTE** — Ctrl+Shift+V düz metin.
- [ ] **Step 5: Kenar durumlar** — iç-içe liste, hücreye yapıştır, seçimi değiştirerek yapıştır, undo.
- [ ] **Step 6: Rollback** — bayraksız stok yapıştırma.
- [ ] **Step 7:** finishing-a-development-branch.

## Öz-İnceleme
Kapsama: PASTERICH(verbatim+RichPaste uyarla)→T1; PASTEIMG(empirik)→T2; PLAINPASTE→T1; bayrak/build/test→T3; CF_HTML(all vs fragment)→T3S3; regresyon→T3S5. Placeholder yok.
